# 竹溪小院 · 一日流光

## 观看

打开 `http://127.0.0.1:8769/preview/?v=8`。自由探索默认从清晨开始，四分钟自然走完完整一天。课文学习按八段原文自动推进镜头与光照，可暂停、跳段和调整朗读速度。右下角显示小院当前时刻。

原有独立成片、环绕、视角预设和手动昼夜面板已从界面移除，只保留“自由探索”和“课文学习”。底层时间接口继续保留。

## 画面变化

- 太阳沿连续轨迹运动，阴影方向和长度随之改变。低角度晨光与夕照偏暖，正午趋于中性明亮。
- 天光、环境反射强度、背景与地面色调共同变化；溪水的反射色和太阳／月光高光同步变化，夜间不会保留白天的亮水面。
- 清晨的薄雾采用低处高度雾与世界空间的溪间雾层，带缓慢漂移，日照增强后自然消散。
- 暮色到来时，三扇窗的柔光和堂屋灯逐渐亮起。投光源位于实际门窗内，现有木格、门扇和器物参与遮挡与投影；窗内光晕保持轻微。
- 夜里保留冷色弱光与月光阴影，让主体可辨，暖光集中在屋内、窗台、门口和邻近庭院。

模型、纹理、雾与灯光均在本项目内程序创建，没有新增下载内容资产。太阳轨迹为适合这座微缩场景的连续艺术化日轨，不是指定经纬度、日期的天文计算。实时效果属于 WebGL 光照近似，并非逐帧离线路径追踪。

本次引擎保存在网页模块中。原有 Blender 工程、GLB 和静态成片继续作为建模／静态交付物，尚未烘焙本轮昼夜动画。

## 后续朗读与分镜接口

新增模块：`preview/daylight.js`。现有预览继续由 `preview/viewer-v2.js` 统一调度，没有第二个独立动画循环。

```js
// 查看任何时刻；时刻归一为 0–24 小时，并固定光照。
viewer.dayCycle.setHour(17.3);

// 自然播放，设置完整一天的现实播放时长（秒，最小 20）。
viewer.dayCycle.setDuration(240);
viewer.dayCycle.play();
viewer.dayCycle.pause();

// 当前光照、晨雾、灯光与播放状态。
viewer.dayCycle.state;

// 无副作用地计算任意时刻的日照参数。
viewer.dayCycle.sample(6.35);

// 外部主时钟精确指定一帧；暂停内置播放。
viewer.seekFrame({
  seconds: 36.5,  // 场景运动的时间，包括风、动物、细波和轻微灯火变化
  hour: 17.3,    // 光照时刻，可传入 25，自动归一至次日 01:00
  camera: {
    position: [-13.8, 12.4, 15],
    target: [0, 1.95, 0],
    zoom: 1
  }
});
```

外部朗读播放器应在每一帧给出明确的 `seconds` 与 `hour`，由同一个音频主时钟计算；不要让音频和光照分别计时。当前 `lesson.js` 已保存八段原文、句子时长、场景目标和光照时间。它按预设朗读节奏推进；用户提供的背景音乐循环播放，不作为课文对齐时钟。将来加入正式朗读人声时，应按音频时间标注替换这些预估时长。

`seconds` 为非负有限数；`hour` 为有限数，允许跨天。`camera` 可省略以保持当前视角，或逐帧传入位置、目标和缩放。镜头缩放范围沿用 0.65–3.2。

背景音效的来源、逐轨生成提示词、混音与时钟方案见同目录《背景音效与朗读联动方案.md》。

## 当前课文控制

```js
viewer.setMode('learn');
viewer.lesson.toggle(false); // 暂停学习
viewer.lesson.jump(4);       // 跳到第 5 段，镜头平滑跟随
viewer.lesson.seek(1, 28);   // 第 2 段内的第 28 秒（预设朗读时间）
viewer.lesson.state;        // 段落、进度、句子、镜头目标和光照时刻
viewer.setMode('explore');
```

当前学习镜头以平滑插值靠近目标。需要精确导出时，用已有 `seekFrame` 接口直接给出每帧的相机和光照值。
