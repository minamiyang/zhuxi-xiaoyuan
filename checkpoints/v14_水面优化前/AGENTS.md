# 竹溪小院优化工作副本

先读 `../交接文档.md`，再按任务选用 `web-3d-modeling-workflow` Skill。

仅修改本工作副本。不得改动相邻留存版或原项目。预览端口 8770，入口 `启动预览.command`；8769 属于原项目。

保留：两种模式、八段课文、清亮水质、自然连贯的昼夜过渡、贴近屋子的完整树冠、四面双排墙基砖、完整木门框、音乐循环和当前轻音效比例。无外部模型/贴图/HDRI；用户提供的音频允许使用。

不要执行 `source/fix_crown_clearance.py`、`source/restore_natural_crown.py` 或旧的整屋包围盒树冠排除检查。不要通过整体移树、缩冠或平面裁切制造屋树间的大空隙。

`source/repair_foundation_and_restore_crown.py` 会重建树冠，未来雕修后不可无条件重跑。旧全量建模脚本会覆盖后续修订；优先局部修改当前成品。

## 历史工作副本版本（2026-09-08）

v13-workcopy 已完成交接文档第一批光影、地表疏密、水石瓦的网页运行时优化，详见 `优化说明.md`。新增 `preview/quality-v3.js`；主工程及动态 GLB 仍保持交接基线，网页最终效果由运行时模块共同实现。相同镜头前后图及当前功能验收在 `verification/质感优化_20260908/`。后续操作以本目录当前文件为基线，勿用历史脚本覆盖。第二批造型精修尚未实施。

## 当前工作副本版本（2026-09-09）

v14-workcopy 已完成第二批瓜叶、树皮、墙面、鸡鸭、餐桌与学习镜头的局部精修，详见 `细节精修与打鸣说明.md`。`preview/details-v14.js` 为新精修模块，`soundscape.js` 增加用户录音 `audio/ambience/rooster.mp3`：场景时间约 05:00 每次经过触发一次，不连续循环。保留配乐 0.26、环境音总增益 0.16。原七轨加打鸣共八轨。课文原文和时序不变；主工程、GLB 未重导出，网页运行时效果为当前成品。当前证据在 `verification/细节精修_20260909/`。v13 说明和图保留为历史记录。
