# 竹溪小院 · 午后有风

参考用户提供的乡居沙盘图片制作的三维微缩场景。主体包括双坡筒瓦屋、透空木格门窗、丝瓜架、竹林、分枝榆树、石拱桥、曲折溪流、母鸡与六只小鸡、两只溪鸭、花境及庭院生活器物。

## 打开作品

- **高清成片**：`renders/竹溪小院_主视角.png`，2560 × 1956；两张局部特写为 1600 × 1250。
- **可编辑工程**：`竹溪小院.blend`，Blender 5.2.1，Cycles 光线追踪。工程保留程序材质、门窗布尔开口、瓦片阵列和植物实例分布。
- **可旋转模型**：`preview/竹溪小院_动态.glb`，可导入兼容 glTF 的三维软件。
- **浏览器预览**：双击 `启动预览.command`，打开本机 `http://127.0.0.1:8769/preview/`。本次已启动此本地服务；重启电脑后可用启动文件恢复。

网页只保留两个模式：

- **自由探索**：拖动旋转、滚轮或双指缩放、右键平移；草木、鸡鸭和昼夜持续变化。
- **课文学习**：展示用户提供的《乡下人家》八段原文，以约每分钟 186 字的基础节奏配合句间停顿自动推进，逐句提示朗读位置。镜头依次关注瓜架、花境、屋后竹林、春笋、鸡群、溪鸭、门前饭菜与月夜灯窗。可暂停、上一段／下一段、直接选择段落，或选择慢一些／正常／快一些。学习模式暂停时仅暂停朗读推进和场景运动，跳段镜头仍会平滑就位。配乐独立循环播放，朗读暂停或全文结束均不中断。退出后恢复自由探索的视角和时光状态。

当前配乐已替换为用户提供的 `配乐终版.mov` 中提取的音轨，默认尝试有声自动播放并循环；右上角可静音。浏览器阻止首次有声自动播放时，会显示“开启配乐”，点击页面或该按钮即可启用。切换到后台会暂停播放，返回后继续。该模式按预设朗读节奏推进，不识别学生实际读到哪里，也不自动播出课文人声。

## 资产与材质

没有使用下载模型、素材库、外部贴图、HDRI、照片贴面或 AI 生成模型。几何、叶片、羽毛、纹理和照明均在本次制作中以程序创建；参考图片仅用于观察，没有贴到模型上。

浏览器使用本地随附的 Three.js 0.180.0 渲染库，这是软件依赖。预览模型包含本次程序生成的便携纹理；微小叶片做了面数精简。浏览器加入有距离变化的柔和阴影、随动作更新的接触遮蔽、实时水面倒影和程序水纹。Cycles 的程序着色、反射与间接光照以 Blender 工程和高清成片为准，浏览器实时渲染的光影与成片存在差异。

背面与室内根据合理建筑关系补全，并非从单张参考图中确定的原样结构。作品采用精细微缩模型的表现方式，未宣称照片级逐像素复原。

## 修改与重建

按中文命名的集合分别编辑建筑、筒瓦、植物、瓜架、庭院器物、动物和地形；隐藏的植物源与布尔控制体属于模型生成结构，请保留。

`source/` 保存本次建模与交付脚本，顺序为：

1. `build_scene.py`：调用 `details.py` 和 `animals.py` 建立基础完整场景。
2. 打开已保存的工程执行 `refine_look.py`、`polish.py`、`surface_pass.py`、`finish_contacts.py`：应用本次检查后的材质、接地与细节修正。
3. 本次材质与光影升级执行 `upgrade_native.py`；执行 `finalize.py` 输出新版高清图；执行 `export_dynamic.py` 输出分离动物组件并含自制颜色、法线、粗糙度贴图的 GLB。导出在独立进程副本中展开实例，不回写覆盖原生模型结构。
4. `preview/viewer-v2.js` 负责预览；`surface-v2.js` 负责软阴影和与表面一致的动态变形；`water-v2.js` 负责实时倒影、波纹和鸭子尾迹；`motion.js` 保存确定性的游动路径与小鸡、风动曲线；`hen-v3.js` 创建母鸡的覆羽、翼尾、头颈与足部，采用刚性头部和随曲线弯曲的颈部协调啄食。母鸡单独模型另存为 `preview/母鸡_重建.glb`，并已同步进主工程。动态在网页中运行，GLB 和 Blender 文件保留可编辑的静态组件，不包含这组网页动画的烘焙时间轴。

`verification/` 保留主要修正前后画面、背面检查和几何/浏览器核验记录。未修改此项目内原有的其他 Blender 工程。

## 昼夜与学习分镜

自由探索从清晨开始，默认四分钟走完一天。学习模式由段落控制时光，连续过渡至晚饭夕照、月夜窗灯，并在末段回望晨光。界面只保留两种游玩模式，底层的时间与逐帧镜头接口仍保留，便于后续与正式朗读音轨或视频导出同步。参见《昼夜引擎使用与接入.md》。

学习模块为 `preview/lesson.js`；配乐模块为 `preview/music.js`，当前音频为 `preview/audio/pei-yue-final.m4a`；程序春笋细节为 `preview/lesson-details.js`。春笋为本轮网页新增几何，未并入静态 Blender 工程。

## 墙脚、门框与树冠修订

四面墙根都有两排连续石砖，转角按墙面衔接，前面按两根木门框立柱的实际外缘留出收口，木柱从底部到门楣完整可见。砖块不与门框包围盒相交。

树冠恢复最初的横向展开和完整叶簇，不再收窄树冠或将它整体移离屋顶。保留自然投影交叠，只微调了紧贴实际曲面瓦片的 139 枚叶片与 1 个枝条控制点。此处不能再用整个屋顶的大包围盒排除全部树冠重叠，那会再次把自然树冠削掉或推远。

当前重建入口为 `source/repair_foundation_and_restore_crown.py`，之后执行 `source/export_dynamic.py`。此前 `fix_crown_clearance.py`、`restore_natural_crown.py` 两个方案都已被替代，不应继续套用。修改已写入主 Blender 工程和网页动态模型；`renders/` 是此前版本的历史静态成片，本次验收以当前网页、工程和 `verification/墙脚树冠配乐修订/` 为准。

新版网页：`http://127.0.0.1:8769/preview/?v=12`。

## 时段与段落音效

用户提供的七种音效已复制到 `preview/audio/ambience/`，由 `preview/soundscape.js` 根据当前场景混音。背景配乐持续循环，播放器音量为 0.26；环境音效总增益为 0.16，作为轻柔背景，音乐更突出。配乐与环境音效各有独立开关。

自由探索：清晨鸟鸣；上午母鸡和小鸡；午后溪鸭；傍晚归巢鸟鸣；夜间虫鸣。小河流水作为低音量环境底声，交界时渐变混合。

课文学习：瓜架、花境配轻鸟鸣；描写竹林与春笋时保留轻鸟鸣；第 4 段为母鸡与小鸡；第 5 段鸭叫与小河流水；第 6 段归巢鸟鸣；第 7 段夜间虫鸣；末段随光照回望小院。暂停或结束朗读不停止配乐和当前环境声，跳段后音效平滑切换。

音轨做了按源响度和峰值限制的增益配平，切换渐入渐出，循环尾首交叉淡化。切至后台暂停声音，返回继续；首次有声播放受浏览器限制时，点击页面或开启音效按钮解锁。页面会显示当前主要音效。

夜间使用的是用户目录内名为“夜间蟋蟀鸣叫.mp3”的音轨，作为夜间虫鸣气氛，不将它标注为真实纺织娘物种录音。原始素材未改动。

竹林风声、捣衣声已从播放列表和网页素材中移除。新增用户提供的“小河流水.mp3”；自由探索持续轻声播放，课文学习在溪鸭段落略微突出，其余段落仅保留很轻的流水底声。用户桌面原始文件未改动。
